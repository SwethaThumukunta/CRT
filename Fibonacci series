#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,t1=1,t2=1,nextterm,i;
    scanf("%d",&n);
    for(i=3;i<=n;i++){
        nextterm=t1+t2;
        t1=t2;
        t2=nextterm;
    }
    printf("%d",t2);
    
    return 0;
}
