#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int s,e,n;
    scanf("%d%d%d",&n,&s,&e);
    int a[n],i;
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        if(a[i]>s && a[i]<e){
         printf("%d ",a[i]);
        }
    }

    
    

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
